import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },

  botaoTextReg: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#fff',
  },

  containerForm: {
    alignItems: 'center',
  }
});

